# ============================================================
# AgentFactory Configuration
# Copy this file to .env and fill in your values.
# ============================================================

# --- LLM API Keys ---
# Gemini (free tier) — recommended default
GEMINI_API_KEY=your-gemini-api-key-here

# OpenAI (paid fallback)
OPENAI_API_KEY=your-openai-api-key-here

# Anthropic (premium fallback)
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# --- Search (optional) ---
# Tavily for web research (free tier available)
TAVILY_API_KEY=your-tavily-api-key-here

# --- Observability (optional) ---
LANGFUSE_SECRET_KEY=your-langfuse-secret-key
LANGFUSE_PUBLIC_KEY=your-langfuse-public-key
LANGFUSE_HOST=https://cloud.langfuse.com

# --- Repository Paths ---
# Absolute paths to your separate repos
BACKEND_PATH=/absolute/path/to/your/fastapi-backend
FRONTEND_PATH=/absolute/path/to/your/react-frontend
ADMIN_PATH=/absolute/path/to/your/admin-panel

# --- Notifications ---
# Discord webhook URL for approval notifications
DEV_NOTIF_WEBHOOK_URL=https://discord.com/api/webhooks/your-webhook-id/your-webhook-token

# Gmail (for email notifications)
GMAIL_USER=your-email@gmail.com
GMAIL_APP_PASSWORD=your-16-char-app-password
ADMIN_EMAIL=recipient@example.com

# --- Approval Server ---
APPROVAL_SERVER_HOST=0.0.0.0
APPROVAL_SERVER_PORT=8000

# --- JWT Authentication (optional, for approval server) ---
# Set this to enable JWT authentication on the approval server
# Generate one with: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=your-jwt-secret-key-here
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24
JWT_AUDIENCE=agentfactory
APPROVAL_DB_PATH=~/.agentfactory/approval.db

# --- LLM Configuration ---
AGENT_DAILY_BUDGET_USD=5.00
LLM_TEMPERATURE=0.2

# --- MCP ---
# Path to mcp.json config file (relative to project root)
MCP_CONFIG_FILE=mcp.json
MCP_SERVERS_DIR=mcp_servers

# --- Persistent Memory ---
# Path to SQLite memory database (default: ~/.agentfactory/memory.db)
MEMORY_DB_PATH=/absolute/path/to/memory.db

# Default agent ID for memory isolation
MEMORY_AGENT_ID=default
